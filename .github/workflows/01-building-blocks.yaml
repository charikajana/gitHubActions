name: 01- Building-Blocks
  on: push
  jobs:
    echo-hello:
      runs-on: ubuntu-latest
      steps:
          -name: SayHello
          run: echo "hello world"
    echo-goodBy:
        runs-on: ubuntu-latest
        steps:
            -name: SayGoodBye
            run: |
            